@model IEnumerable<PL.Model.POCO.Bem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoBreve)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoCompleta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoBreve)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoCompleta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BemId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BemId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BemId })
        </td>
    </tr>
}

</table>
